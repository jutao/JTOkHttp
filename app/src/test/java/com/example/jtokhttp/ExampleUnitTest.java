package com.example.jtokhttp;

import com.example.service.MyAPIProvider;
import com.example.service.MyRequest;
import com.example.service.MyResponse;
import java.util.HashMap;
import java.util.Map;
import org.junit.Test;

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {
  @Test public void addition_isCorrect() throws Exception {
    //HttpRequest httpRequest = HttpRequestProvider.getHttpRequest(
    //    new RequestParameters("http://localhost:8080/web/HttpServlet", HttpMethod.POST));
    //if (httpRequest != null) {
    //  Map<String, String> body = new HashMap<>(2);
    //  body.put("username", "jutao");
    //  body.put("userage", "55");
    //  HttpResponse response = httpRequest.execute();
    //  String content = null;
    //  BufferedReader reader = new BufferedReader(new InputStreamReader(response.getBody()));
    //  while ((content = reader.readLine()) != null) {
    //    System.out.println(content);
    //  }
    //  reader.close();
    //}

  }
}